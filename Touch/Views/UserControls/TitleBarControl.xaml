<UserControl
    x:Class="Touch.Views.UserControls.TitleBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="32" Background="Transparent">
        <Rectangle x:Name="TitleBar" Fill="Transparent" />
        <StackPanel Orientation="Horizontal">
            <Button x:Name="BackButton" Width="48" VerticalAlignment="Stretch" Style="{StaticResource ListButtonStyle}"
                    Visibility="Collapsed">
                <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{StaticResource Back}"
                          FontSize="12" Foreground="Black" />
            </Button>
            <TextBlock x:Name="TitleText" Margin="8" Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>
    </Grid>
</UserControl>